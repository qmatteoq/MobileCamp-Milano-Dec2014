<Page
    x:Class="Caliburn_AdvancedDemo.Views.DetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Caliburn_AdvancedDemo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer>
            <StackPanel Margin="12, 0, 12, 0">
                <TextBlock x:Name="Title" Style="{StaticResource HeaderTextBlockStyle}" TextWrapping="Wrap" />
                <TextBlock x:Name="Summary" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
